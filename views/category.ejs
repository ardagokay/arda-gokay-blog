<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= currentCategory %> Kategorisi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <header class="bg-white shadow-sm">
        <div class="container mx-auto px-4 py-6">
            <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
                <h1 class="text-3xl font-bold text-gray-800">BlogSitem</h1>
                
                <div class="flex flex-col md:flex-row items-center space-y-4 md:space-y-0 md:space-x-6">
                    <form action="/search" method="GET" class="flex">
                        <input type="text" name="q" placeholder="Yazılarda ara..." 
                               class="px-4 py-2 border border-gray-300 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-blue-500 w-48 md:w-64">
                        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-r-lg hover:bg-blue-700">
                            <i class="fas fa-search"></i>
                        </button>
                    </form>
                    
                    <nav class="hidden md:flex space-x-6">
                        <a href="/" class="text-gray-600 hover:text-blue-600">Ana Sayfa</a>
                        <% categories.forEach(category => { %>
                        <a href="/category/<%= category.slug %>" class="text-gray-600 hover:text-blue-600 <%= currentCategory === category.slug ? 'text-blue-600 font-bold' : '' %>"><%= category.name %></a>
                        <% }); %>
                        <a href="/admin" class="text-blue-600 hover:text-blue-800">Yönetim</a>
                    </nav>
                </div>
            </div>
        </div>
    </header>

    <main class="container mx-auto px-4 py-8">
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-800"><%= currentCategory %> Kategorisi</h1>
            <p class="text-gray-600 mt-2"><%= posts.length %> yazı bulundu</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% posts.forEach(post => { %>
            <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow">
                <% if(post.image) { %>
                <div class="w-full h-48 overflow-hidden">
                    <img src="<%= post.image %>" alt="<%= post.title %>" class="w-full h-full object-cover">
                </div>
                <% } %>
                <div class="p-6">
                    <span class="inline-block bg-blue-100 text-blue-800 text-sm px-3 py-1 rounded-full mb-3">
                        <%= post.category %>
                    </span>
                    <h2 class="text-xl font-bold text-gray-800 mb-3">
                        <%= post.title %>
                    </h2>
                    <p class="text-gray-600 mb-4 line-clamp-3">
                        <%= post.content.substring(0, 150) %>...
                    </p>
                    <div class="flex justify-between items-center text-sm text-gray-500">
                        <span><%= post.createdAt.toLocaleDateString('tr-TR') %></span>
                        <a href="/post/<%= post._id %>" class="text-blue-600 hover:text-blue-800 font-medium">
                            Devamını Oku →
                        </a>
                    </div>
                </div>
            </article>
            <% }); %>
        </div>

        <% if(posts.length === 0) { %>
        <div class="text-center py-12">
            <p class="text-gray-500 text-lg">Bu kategoride henüz yazı bulunmuyor.</p>
        </div>
        <% } %>
    </main>

    <footer class="bg-gray-800 text-white mt-12">
        <div class="container mx-auto px-4 py-8">
            <div class="text-center">
                <p>&copy; 2024 BlogSitem. Tüm hakları saklıdır.</p>
            </div>
        </div>
    </footer>
</body>
</html>